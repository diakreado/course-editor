<!DOCTYPE html>
<html lang="en">

<head>
    <% include particular/head %>
        <title>
            <%= title %>
        </title>
</head>

<body>
    <h1>
        <%= title %>
    </h1>
    <p>
        <form action="/create">
            <button type="submit" class="btn btn-primary my-1">Add new</button>
        </form>
    </p>
    <ul>
        <% items.forEach(function(item) { %>
            <li>
                <%= item %>
            </li>
            <% }); %>
    </ul>
</body>

</html>